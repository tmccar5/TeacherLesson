<% @lessons.each do |lesson| %>
    <div class="box panel panel-default">
        <div class="panel-body">
          <h2>
            <%= link_to lesson.date, lesson %>
          </h2>
        </div>
    </div>
  <% end %> 

<%= render 'form' %>